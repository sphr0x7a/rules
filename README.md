## 分流規則
該 branch 係 surge 嘅 `domain-set` 同 `rule-set`，`rule-set` 得 `ip-cidr` 同 `ip-cidr6`
`sample.conf` 中得 rules 嘅板塊

另一叫 mihomo 嘅 branch 係 mihomo 嘅 `domain-set` 同 `ip-set`，內容同 main branch 完全一樣，只有格式上嘅差別
